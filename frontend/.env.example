## ThreatSim frontend (Vite/React) environment
## Copy to `frontend/.env` and fill values as needed.

# Supabase client config (optional).
# These are safe to be in the browser (Anon key), but still treat them as config.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

